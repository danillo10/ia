# Minutask — App Contratante (Flutter)

Execute após instalar o [Flutter SDK](https://docs.flutter.dev/get-started/install):

```bash
flutter create . --project-name minutask_contratante --org br.com.minutask
flutter pub get
flutter run
```

Ajuste `lib/core/config.dart` com a URL da API.
