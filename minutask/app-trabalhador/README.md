# Minutask — App Trabalhador (Flutter)

Modelo Uber: ficar online, receber ofertas, aceitar ou recusar.

```bash
flutter create . --project-name minutask_trabalhador --org br.com.minutask
flutter pub get
flutter run
```

Cadastro com foto do documento: `POST /auth/cadastro` (multipart).
